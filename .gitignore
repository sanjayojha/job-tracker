# The DDEV environment is per-machine and not tracked.
# Recreate it from the "Local setup" section of README.md.
/.ddev/

# Personal Claude Code notes; CLAUDE.md itself is tracked.
CLAUDE.local.md

# Scratch output from the Playwright MCP server.
.playwright-mcp/

# Stray root .env if DDEV's laravel settings step writes one here;
# the real one lives at backend/.env
/.env

.DS_Store
*.swp
.idea/
